% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rosales_example.R
\docType{data}
\name{rosales_example}
\alias{rosales_example}
\title{Example Rosales occurrence data}
\format{
A data.table with 31,742 rows and 4 variables:
\describe{
\item{accepted_name}{Character. Accepted taxonomic name of the species}
\item{longitude}{Numeric. Longitude in decimal degrees (-180 to 180)}
\item{latitude}{Numeric. Latitude in decimal degrees (-90 to 90)}
\item{data_source}{Character. Source of the occurrence record (e.g., "GBIF", "BIEN")}
}
}
\source{
Derived from native Rosales occurrence records compiled from GBIF and BIEN databases.
Data were preprocessed following the workflow in \code{build_species_alpha_hull_1deg_ranges.R}.
}
\usage{
rosales_example
}
\description{
A dataset containing occurrence records for 100 Rosales species, suitable for
testing and demonstrating alpha-hull range construction. Species were selected
from a larger Rosales dataset with occurrence counts between 50 and 3000 to
ensure computations complete in reasonable time.
}
\details{
The dataset includes 100 species from the Rosales order with the following characteristics:
\itemize{
\item All species have between 50 and 2,204 occurrence records
\item Records have been cleaned and deduplicated by (accepted_name, longitude, latitude)
\item Coordinates have been validated to be within valid ranges
\item Species names have been normalized (whitespace trimmed, underscores replaced with spaces)
}

Top species by occurrence count:
\itemize{
\item Urtica urens: 2,204 records
\item Rubus allegheniensis: 1,880 records
\item Holodiscus discolor: 1,591 records
\item Pourthiaea villosa: 1,502 records
\item Humulus scandens: 1,371 records
}
}
\examples{
data(rosales_example)

# Basic summary
nrow(rosales_example)
data.table::uniqueN(rosales_example$accepted_name)

# Species with most records
rosales_example[, .N, by = accepted_name][order(-N)][1:10]

\dontrun{
# Build an alpha-hull range for one species and find its occupied 1-degree cells
library(terra)
data(grid_1deg)
grid <- unwrap(grid_1deg)

species_data <- rosales_example[rosales_example$accepted_name == "Rosa canina", ]
range <- getDynamicAlphaHull(
  species_data,
  coordHeaders = c("longitude", "latitude"),
  clipToCoast = "terrestrial"
)
plot(range$hull, col = "lightblue", border = "steelblue")
points(species_data$longitude, species_data$latitude, pch = 16)

# Intersect the range with the 1-degree grid
pairs <- terra::relate(grid, range$hull, relation = "intersects", pairs = TRUE)
occupied_cells <- unique(grid$grid_id[pairs[, 1]])
cat("Occupied cells:", length(occupied_cells), "\n")
}
}
\keyword{datasets}
